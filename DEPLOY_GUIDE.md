'''# 🚀 دليل النشر النهائي: شغّل بوتك مجاناً على سيرفر دائم

مرحباً! هذا هو الدليل النهائي والبسيط جداً لتشغيل بوتك بشكل دائم ومجاني. سنستخدم سيرفر **Render** الذي سيعمل 24/7.

**العملية كلها تستغرق 10-15 دقيقة فقط.**

---

## 🎯 الهدف

1.  **إنشاء مستودع (Repository)** على حسابك في GitHub.
2.  **رفع ملفات البوت** التي جهزتها لك إلى هذا المستودع.
3.  **ربط المستودع بسيرفر Render** المجاني.

---

## الخطوة 1: رفع الكود إلى GitHub (5 دقائق)

### 1.1 إنشاء مستودع جديد

1.  اذهب إلى [github.com/new](https://github.com/new).
2.  **اسم المستودع (Repository name)**: اكتب `chiqnews-telegram-bot`.
3.  **الوصف (Description)**: اختياري، يمكنك كتابة `My Telegram News Bot`.
4.  تأكد من أنه **عام (Public)**.
5.  **لا تحدد** أي خيار من (Add a README file, Add .gitignore, Choose a license).
6.  اضغط على زر **"Create repository"** الأخضر.

### 1.2 رفع ملفات البوت

لقد استلمت ملفاً مضغوطاً باسم `simple_bot.tar.gz`. قم بفك ضغطه أولاً.

1.  في صفحة المستودع الجديدة التي أنشأتها على GitHub، اضغط على رابط **"uploading an existing file"**.
2.  ستفتح لك صفحة جديدة. **اسحب جميع الملفات** التي كانت داخل مجلد `simple_bot` وأفلتها في هذه الصفحة.
    *   الملفات هي: `bot.py`, `.env`, `requirements.txt`, `Procfile`, `runtime.txt`, `.gitignore`, `GET_KEYS.md`, `QUICK_START.md`.
3.  انتظر حتى يتم تحميل جميع الملفات.
4.  في الأسفل، اضغط على زر **"Commit changes"** الأخضر.

**ممتاز! الآن كود البوت موجود على حسابك في GitHub.**

---

## الخطوة 2: إعداد السيرفر على Render (5 دقائق)

### 2.1 إنشاء حساب وربطه بـ GitHub

1.  اذهب إلى [dashboard.render.com](https://dashboard.render.com).
2.  اضغط على زر **"Sign in with GitHub"**.
3.  سيطلب منك Render السماح بالوصول إلى حسابك في GitHub. وافق على ذلك (Authorize Render).

### 2.2 إنشاء خدمة جديدة للبوت

1.  في لوحة تحكم Render، اضغط على زر **"New +"** واختر **"Background Worker"**.
2.  ستظهر لك قائمة بمستودعات GitHub الخاصة بك. ابحث عن مستودع `chiqnews-telegram-bot` واضغط على **"Connect"** بجانبه.
3.  ستفتح صفحة إعدادات الخدمة. املأها كالتالي:
    -   **Name**: اكتب `chiqnews-bot` (أو أي اسم تريده).
    -   **Region**: اتركه كما هو.
    -   **Branch**: اتركه `master` أو `main`.
    -   **Runtime**: اتركه `Python 3`.
    -   **Build Command**: اتركه `pip install -r requirements.txt`.
    -   **Start Command**: اكتب `python bot.py`.
    -   **Plan**: اختر الخطة **المجانية (Free)**.

4.  لا تضغط على زر الإنشاء بعد! انتقل للخطوة التالية.

---

## الخطوة 3: إضافة المتغيرات السرية (أهم خطوة)

هذه هي أهم خطوة لكي يعمل البوت. سنقوم بنسخ محتويات ملف `.env` إلى إعدادات Render.

1.  في نفس صفحة إعدادات Render، انزل إلى قسم **"Environment"**.
2.  اضغط على **"Add Environment Variable"**.
3.  ستقوم بإضافة **8 متغيرات** واحداً تلو الآخر. لكل متغير، أدخل **"Key"** و **"Value"** من ملف `.env` الخاص بك.

| Key (المفتاح) | Value (القيمة) |
| :--- | :--- |
| `TELEGRAM_PHONE` | `+9647734446636` |
| `OPENAI_API_KEY` | `sk-proj-cASio...` (المفتاح الكامل) |
| `SOURCE_CHANNELS` | `AjaNews` |
| `DESTINATION_CHANNEL` | `@AjeelNewsIq` |
| `REWRITE_STYLE` | `professional` |
| `TELEGRAM_API_ID` | القيمة التي حصلت عليها من my.telegram.org |
| `TELEGRAM_API_HASH` | القيمة التي حصلت عليها من my.telegram.org |
| `BOT_TOKEN` | القيمة التي حصلت عليها من BotFather |

**ملاحظة:** بعد إضافة كل متغير، اضغط على "Add" ثم "Add Environment Variable" مرة أخرى لإضافة التالي.

---

## الخطوة 4: التشغيل النهائي (دقيقة واحدة)

1.  بعد إضافة جميع المتغيرات الثمانية، انزل إلى أسفل الصفحة واضغط على زر **"Create Background Worker"**.
2.  سيبدأ Render الآن في بناء وتشغيل البوت الخاص بك. قد تستغرق هذه العملية بضع دقائق.
3.  يمكنك مشاهدة تقدم العملية مباشرة من قسم **"Logs"**.

**عندما ترى في السجل (Logs) رسالة مثل:**

```
▶️ جاري تشغيل البوت...
👂 البوت يستمع الآن للرسائل من: AjaNews
```

**فهذا يعني أن بوتك يعمل بنجاح على السيرفر بشكل دائم!** 🎉

---

## ماذا بعد؟

-   **التحديثات**: أي تغيير تقوم به في مستودع GitHub سيتم نشره تلقائياً على Render.
-   **السجلات**: يمكنك دائماً العودة إلى صفحة الخدمة في Render لمشاهدة السجلات ومراقبة أداء البوت.
-   **المتغيرات**: يمكنك تعديل المتغيرات (مثل إضافة قنوات مصدر جديدة) من قسم "Environment" في إعدادات Render.

**تهانينا! لقد قمت بنشر بوتك على سيرفر مجاني ودائم بنجاح!** 🚀
''
