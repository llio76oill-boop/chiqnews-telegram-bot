# 🚀 دليل التشغيل السريع جداً (3 خطوات فقط)

مرحباً! هذا دليل مختصر جداً لتشغيل البوت. فقط انسخ الأوامر والصقها.

---

## الخطوة 1: املأ 3 فراغات فقط

لقد قمت بملء كل شيء تقريباً. تبقى 3 أشياء فقط تحتاج إلى إضافتها بنفسك.

1.  **اذهب إلى [my.telegram.org](https://my.telegram.org)** واحصل على `API_ID` و `API_HASH`.
2.  **تحدث مع [@BotFather](https://t.me/BotFather)** واحصل على `BOT_TOKEN` لبوتك `chiqnewsbot`.
3.  **افتح ملف `.env`** وأدخل هذه القيم الثلاثة في الأماكن المخصصة لها.

**شكل ملف `.env` بعد التعديل:**
```ini
# ... (القيم التي أدخلتها أنا)

# ---------------------------------------------------
# ⚠️ يرجى ملء هذه القيم الثلاثة بنفسك
# ---------------------------------------------------
TELEGRAM_API_ID=12345678
TELEGRAM_API_HASH=abcdef1234567890abcdef1234567890
BOT_TOKEN=1234567890:ABC-DEF1234ghIkl-zyx57W2v1u1234567890
```

---

## الخطوة 2: تثبيت المكتبات

افتح نافذة الأوامر (Terminal) ونفذ الأمرين التاليين:

```bash
# الأمر الأول: تثبيت كل المكتبات المطلوبة
pip install -r requirements.txt
```

---

## الخطوة 3: تشغيل البوت

الآن، نفذ الأمر الأخير:

```bash
# الأمر الثاني: تشغيل البوت
python bot.py
```

**في المرة الأولى فقط**، سيطلب منك تليغرام تسجيل الدخول:
-   أدخل **رمز التحقق** الذي سيصلك على حسابك في تليغرام.
-   إذا طلب **كلمة مرور**، أدخل كلمة مرور التحقق بخطوتين الخاصة بك.

**هذا كل شيء!** 🎉

البوت الآن يعمل وسيستمع للرسائل من قناة `AjaNews` وينشرها بعد إعادة صياغتها في قناتك `@AjeelNewsIq`.

---

### ملاحظات هامة:

-   **لا تغلق نافذة الأوامر**، وإلا سيتوقف البوت. إذا أردت تشغيله بشكل دائم، ستحتاج إلى رفعه على سيرفر (خادم).
-   تأكد من أن بوتك `chiqnewsbot` هو **مشرف (Admin)** في قناتك `@AjeelNewsIq` ولديه صلاحية **نشر الرسائل (Post Messages)**.
